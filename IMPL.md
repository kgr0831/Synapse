# Synapse — 구현 계획

> `PLAN.md` = 무엇을 만드는가 (제품·사이트 기획)
> `DESIGN.md` = 어떻게 보이는가 (비주얼 시스템)
> `IMPL.md` = **어떻게 만드는가 (엔지니어링 계획)** ← 이 문서
>
> 상태: v0.1 / 마일스톤 M0 착수 전

---

## 1. 규모에 대한 정직한 평가

먼저 이것부터 인정하고 시작합니다.

| 구성요소 | 실제 정체 | 1인+AI 집중 작업 추정 |
|---|---|---|
| Hub | 인증·스토리지·CDN·샌드박스 호스팅·계측을 갖춘 플랫폼 | 7~10주 |
| Engine | WebGPU 3D 렌더러 + ECS + 에셋 파이프라인 + 물리 | 8~12주 |
| Graph + Compiler | 노드 포맷·검증·TS/WGSL 코드젠 | 6~8주 |
| Studio | 브라우저 3D 에디터 (기즈모·인스펙터·노드 캔버스) | 10~14주 |
| AI Layer | MCP 서버 + RAG + 결정적 헤드리스 검증 | 5~7주 |

합계 **36~51주**입니다. 풀타임이면 9~12개월, 현실적인 병행 작업이면 18개월 이상입니다. 그리고 이 추정에는 운영·버그·재작업이 빠져 있습니다.

**여기서 나오는 결론은 "빨리 하자"가 아니라 "덜 만들자"입니다.**

---

## 2. 세 가지 실행 원칙

### 원칙 1 — 인프라는 사고, 제품은 만든다

차별점이 없는 곳에 시간을 쓰면 차별점이 늦어집니다.

| 영역 | 결정 | 이유 |
|---|---|---|
| 인증 | **산다** (Clerk 또는 Supabase Auth) | 우리 제품의 어떤 부분도 인증으로 차별화되지 않음 |
| DB | **산다** (Neon 또는 Supabase Postgres) | 운영 부담 제거. pgvector가 P5에 필요 |
| 오브젝트 스토리지 + CDN | **산다 — Cloudflare R2** | §10 참조. 아웃바운드 요금이 이 제품의 최대 비용 항목이고 R2는 그걸 청구하지 않음 |
| 샌드박스 실행 | **산다** (Cloudflare Workers + 와일드카드 서브도메인) | 격리는 플랫폼에 맡기는 게 직접 짜는 것보다 안전 |
| 헤드리스 캡처 | **만든다** | GPU가 붙은 워커가 필요. 기성품 없음 |
| 빌드 파이프라인 | **만든다** | 제품 고유 |
| 엔진·그래프·Studio·MCP | **만든다** | 여기가 제품 |

### 원칙 2 — 각 마일스톤은 그 자체로 쓸모가 있어야 한다

엔진을 다 만든 뒤 Studio를 만들면 8~12주 동안 검증이 0입니다. 대신:

- **엔진의 첫 산출물은 Studio가 아니라 npm 패키지**입니다. `@synapse/engine`으로 손으로 쓴 TS 게임을 만들 수 있게 하고, 그 게임을 Hub에 올려 실제 사용자 기기에서 돌립니다.
- 그래프 컴파일러의 첫 산출물은 에디터가 아니라 **CLI**입니다. `graph.json` → TS 변환이 커맨드라인에서 되면, Studio는 그 위의 UI일 뿐입니다.

이렇게 하면 모든 마일스톤이 Hub를 통해 실사용자에게 노출됩니다.

### 원칙 3 — 리스크를 먼저 사서 확인한다

가장 늦게 발견하면 가장 비싼 것 순서로 M0에서 스파이크를 칩니다.

---

## 3. M0 — 리스크 스파이크 (1~2주, 최우선)

각 스파이크는 **시간 상자**가 있고, 실패 시 대응책이 미리 정해져 있습니다. 코드 품질은 신경 쓰지 않습니다. 답만 얻습니다.

### S1. 헤드리스 WebGPU 캡처 — **가장 위험**

퍼블리시 전 리포트(스크린샷·폴백 영상·첫 프레임 시간·셰이더 실패)는 P1의 차별점 전체입니다. 그런데 이게 되려면 **헤드리스 브라우저에서 WebGPU가 돌아야** 합니다.

- 확인할 것: Playwright + Chrome headless에서 `navigator.gpu`가 살아 있는가. GPU 없는 리눅스 컨테이너에서 소프트웨어 백엔드(SwiftShader/Dawn)로라도 렌더되는가. 프레임 캡처와 영상 인코딩이 되는가.
- 시간 상자: **3일**
- 성공: 실제 WebGPU 페이지가 헤드리스에서 렌더되고 PNG가 나온다
- **중요한 구분:** 소프트웨어 백엔드로 돌면 *페이로드·로딩 시간·셰이더 컴파일 성공 여부*는 유효하지만 *프레임 타이밍*은 무의미합니다. 리포트에서 이 둘을 반드시 분리해 표기해야 합니다. 섞어서 보여주면 거짓말이 됩니다.

#### 결과 (Windows 로컬, `npm run spike:s1`)

| 구성 | `navigator.gpu` | `requestAdapter()` |
|---|---|---|
| 헤드리스 Chromium (플래그 5종 조합) | 존재 | **null** |
| 헤드리스 시스템 Chrome | 존재 | **null** |
| **창 있는 시스템 Chrome** | 존재 | **성공** — amd/rdna-2, 기능 22개, 벤치 평균 16.7ms |

**결론: 막는 것은 헤드리스가 아니라 디스플레이의 부재입니다.** API 표면은 헤드리스에서도 살아 있는데 어댑터만 안 잡힙니다. 그리고 창이 있을 때 잡힌 것은 소프트웨어가 아니라 **실제 하드웨어 어댑터**였습니다.

따라서 원래 세워둔 대응책("GPU VM으로 분리")으로 갈 필요가 없습니다.

#### 판정 (GitHub Actions, ubuntu-latest + Xvfb) — **PASS**

`.github/workflows/spike-s1.yml`이 무료 Ubuntu 러너에서 Xvfb 가상 디스플레이를 띄우고 스파이크를 돌립니다. **어댑터가 잡힙니다.**

> 스크립트는 어떤 구성도 어댑터를 못 얻으면 exit 1을 반환하도록 고쳤습니다. 그전에는 실패해도 exit 0이라 CI 초록불이 아무것도 증명하지 못했습니다 — 검증 도구로서 결함이었습니다.

**결론: 캡처 워커는 GitHub Actions에서 무료로 유지됩니다.** §10.3의 3순위 리스크가 해소됐습니다.

**단, ubuntu-latest 러너에는 GPU가 없으므로 이 어댑터는 소프트웨어 래스터라이저입니다.** 따라서 위 "중요한 구분"이 그대로 적용됩니다 — 캡처 워커가 내는 **페이로드·로딩 시간·셰이더 컴파일 결과는 유효하고, 프레임 타이밍은 무효**입니다. 퍼블리시 전 리포트 UI는 이 둘을 다른 출처로 표기해야 하며, 실기기 프레임 데이터는 M2의 텔레메트리(실사용자 세션)에서만 옵니다.

### S2. 어댑터 정보 가용성 — **두 번째로 위험**

호환성 등급표는 브라우저가 어댑터 정보를 얼마나 주느냐에 달려 있습니다. 안 주면 표가 비고, 제품의 핵심 화면이 빈 껍데기가 됩니다.

- 확인할 것: Chrome/Edge/Firefox/Safari 각각에서 `adapter.info`(vendor·architecture·device·description)가 무엇을 반환하는가. 마스킹되는가. `adapter.limits`·`adapter.features`는 온전한가
- 시간 상자: **1일**
- 성공: 최소한 vendor + architecture 수준의 구분이 3개 이상 브라우저에서 가능
- 실패 시: 등급표의 축을 "GPU 모델"에서 **"기능·한계 프로파일"**로 바꾼다. 어차피 개발자에게 더 유용할 수 있음

#### 결과 (1개 기기 · Chromium 계열, `/status/webgpu`)

| `adapter.info` 필드 | 값 |
|---|---|
| `vendor` | `amd` |
| `architecture` | `rdna-2` |
| `device` | **빈 문자열 (마스킹)** |
| `description` | **빈 문자열 (마스킹)** |
| `subgroupMinSize` / `MaxSize` | 32 / 128 |

기능 19개 노출, 스펙 기본값을 넘는 한계 15개, 셰이더 프로브 5종 전부 통과.

**결론: 성공과 실패의 중간입니다.** GPU 모델명("Radeon 780M")은 못 얻지만 **vendor + architecture는 얻습니다.** 그래서 등급표의 축을 이렇게 잡습니다:

> **1차 키 = `vendor/architecture` (예: `amd/rdna-2`), 2차 키 = 기능·한계 프로파일 해시.**

모델명보다 거칠지만 드라이버 세대 단위로는 유효하고, 오히려 크리에이터가 대응하기엔 이 입도가 낫습니다. 랜딩 §03의 등급표 컬럼도 여기에 맞춰 바꿔야 합니다.

**아직 부족한 것:** 측정 기기가 **1대뿐**입니다. Firefox·Safari·모바일에서 무엇이 마스킹되는지는 미확인이고, 진단 페이지를 배포해 리포트를 모아야 확정됩니다.

### S3. 샌드박스 격리 (무료 구간 전제)

도메인을 사지 않으므로 사이트는 `*.pages.dev`, 게임은 `*.r2.dev`에서 서빙합니다. 서로 다른 오리진이라는 점이 격리의 근거입니다 (§10.2).

- 확인할 것: `r2.dev`에서 실행되는 번들이 (a) 부모 프레임 DOM 접근 불가 (b) 우리 API 호출 불가(CORS) (c) 우리 도메인 쿠키/스토리지 접근 불가 (d) 자기 오리진에서는 정상 동작
- 시간 상자: **2일**
- 성공: 위 4개를 침투 테스트 스크립트로 확인
- 실패 시: `<iframe sandbox>` 속성 조합으로 보강
- **범위 명시:** 이 단계에서 확보되는 것은 *사이트↔게임* 격리입니다. *게임↔게임* 격리는 와일드카드 서브도메인이 필요하고 도메인 구입 시점까지 미룹니다

### S4. 번들 스펙 실사용 검증

- 확인할 것: three.js·Babylon.js·자체 WGSL로 만든 실제 게임 3개를 **수정 없이** 업로드해 실행
- 시간 상자: **2일**
- 성공: 3개 다 무수정 실행. 안 되면 무엇이 걸렸는지 목록화 → 스펙에 반영
- 이 스파이크가 §6의 `bundle.json` 스펙을 확정시킵니다

> S5(Rapier3D 결정론)는 M3 직전에 칩니다. 지금은 답이 필요 없습니다.

---

## 4. 기술 스택 — 무료로 시작한다

**제약: 지출 0원으로 간다.** 이건 타협이 아니라 현 단계에 맞는 선택입니다. 유료 인프라가 필요해지는 시점은 곧 사용자가 생겼다는 뜻이고, 그때 결정하면 됩니다.

저장소가 **public**이라는 점이 이 전략의 토대입니다 — GitHub Pages와 Actions 무료 분 무제한이 여기 걸려 있습니다.

### 4.1 2단계 배포 전략

| | 단계 A — 지금 (M0) | 단계 B — Hub 착수 시 (M1~) |
|---|---|---|
| 호스팅 | **GitHub Pages** | **Cloudflare Pages** |
| 이유 | 새 계정 0개. 저장소만으로 오늘 배포됨 | 서버리스 함수·스토리지·DB가 한 벤더에 |
| 한계 | 서버사이드 없음 → 업로드·인증 불가 | — |

단계 A는 정적 페이지(랜딩 + `/status/webgpu` 진단)만 필요한 M0 동안 유효합니다. 업로드가 필요해지는 순간 단계 B로 넘어가며, 이전 비용은 워크플로 파일 하나뿐입니다.

### 4.2 단계 B 스택 (전부 무료 구간에서 시작)

| 층 | 선택 | 무료 구간 | 유료로 넘어가는 조건 |
|---|---|---|---|
| 정적 호스팅 | Cloudflare Pages | 대역폭 무제한 | 사실상 없음 |
| API·라우팅 | Cloudflare Workers | 10만 요청/일 | §10 참조 — 설계로 회피 |
| 번들 스토리지 | **Cloudflare R2** | 10GB 저장 · **아웃바운드 영구 무과금** | **저장 10GB 초과 ← 가장 먼저 깨짐** |
| DB | Cloudflare D1 (또는 Supabase/Neon) | 수 GB | 한참 뒤 |
| 인증 | Auth.js on Workers (GitHub OAuth) | 무제한 (자체 호스팅) | 없음. 대신 구현 공수 |
| 캡처 워커 | **GitHub Actions** | public 저장소는 무료 무제한 | GPU 필요 판명 시 (S1) |
| 이메일 | 필요해질 때 | — | 매직링크 대신 OAuth만 쓰면 불필요 |

인증에서 Clerk/Supabase 대신 **Auth.js + GitHub OAuth**를 고른 이유: 1차 사용자(P0 페르소나)가 전부 개발자라 GitHub 계정이 있고, MAU 상한이라는 미래의 과금 트리거를 아예 없앨 수 있습니다. §2 원칙 1("인증은 산다")에서 벗어나는 예외이며, 근거는 무료 제약입니다.

### 4.3 만드는 것 (변동 없음)

| 층 | 선택 |
|---|---|
| 엔진 | TypeScript + WGSL. WASM은 물리(Rapier)만 |
| 그래프 문서 | Loro 또는 Yjs (CRDT), M4부터 |
| 번들러 | Vite / esbuild (도입 완료) |
| MCP | 공식 TypeScript SDK, Streamable HTTP |

**미결**: 웹 프레임워크. Studio의 캔버스 중심 UI에는 Svelte의 오버헤드가 적지만 생태계와 AI 학습 데이터는 Next.js가 큽니다. Cloudflare Workers 배포는 양쪽 다 지원합니다. **M1 착수 시 결정**하고 그 전까지 코드를 프레임워크에 묶지 않습니다.

> 무료 구간 수치는 벤더가 수시로 바꿉니다. 위 숫자는 계획 수립 시점 기준이며 **가입 시점에 반드시 재확인**합니다.

---

## 5. 저장소 구조

지금은 단일 패키지입니다. 워크스페이스는 **필요해질 때** 도입합니다(M3).

```
현재                          M3 이후
─────────                     ─────────────────────────────
web/index.html                apps/
vite.config.js                  web/          Hub + Studio (SvelteKit/Next)
package.json                    capture/      헤드리스 캡처 워커
brand/                          mcp/          MCP 서버 (M6)
                              packages/
                                engine/       @synapse/engine
                                graph/        포맷 + 컴파일러 + CLI
                                bundle/       번들 스펙 + 검증기
                                telemetry/    클라이언트 SDK
                              brand/
                              docs/           /docs 원본 = RAG 코퍼스
```

---

## 6. 지금 얼려야 하는 인터페이스 4개

나중에 바꾸면 데이터 마이그레이션이 필요한 것들입니다. **M1 전에 스펙을 확정하고 버전 필드를 넣습니다.**

### 6.1 `bundle.json` — 업로드 번들 매니페스트

```json
{
  "spec": 1,
  "name": "Orbital Drift",
  "entry": "index.html",
  "engine": { "name": "three.js", "version": "r180" },
  "canvas": { "mode": "fill", "aspect": "16:9" },
  "input": ["keyboard", "mouse", "gamepad"],
  "requires": {
    "webgpu": true,
    "features": ["depth32float-stencil8"],
    "limits": { "maxStorageBufferBindingSize": 134217728 }
  },
  "minSpec": "Discrete GPU or Apple Silicon",
  "save": "local",
  "telemetry": true
}
```

`requires` 블록이 능력 게이트의 전부입니다. 플레이 페이지는 이 값과 실제 어댑터를 대조해 **실행 전에** 폴백 여부를 결정합니다. 매니페스트가 없으면 자동 추론 후 사용자에게 확인받습니다(강제 작성 금지).

### 6.2 텔레메트리 이벤트

```json
{
  "v": 1, "release": "rel_...", "session": "01J...",
  "adapter": { "vendor": "...", "architecture": "...", "description": "..." },
  "features": ["..."], "limits": { "...": 0 },
  "boot": { "ttfbMs": 0, "assetsMs": 0, "firstFrameMs": 0, "bytes": 0 },
  "frames": { "n": 0, "avg": 0, "p50": 0, "p95": 0, "p99": 0 },
  "events": [ { "tMs": 0, "kind": "device_lost" } ]
}
```

`kind`는 `device_lost` · `shader_error` · `oom` · `context_lost`만. 자유 문자열 금지 — 집계가 불가능해집니다.

### 6.3 `graph.json` / `layout.json` (M4에서 쓰지만 스펙은 지금)

```json
{ "spec": 1,
  "nodes": [ { "id": "01J...", "type": "core/on_start", "pins": { "count": 4 } } ],
  "edges": [ { "from": ["01J...", "exec"], "to": ["01J...", "exec"] } ] }
```

의미와 좌표를 분리하고, ID는 ULID, 직렬화는 키 정렬 + 고정 정밀도. 근거는 PLAN.md §6.4.

### 6.4 MCP 툴 스키마

PLAN.md §7.2의 8개 연산. **파일 읽기/쓰기 툴은 넣지 않습니다.** 스키마를 M6까지 미루지 않는 이유는 §8에 있습니다.

---

## 7. 마일스톤

각 작업에 검증 기준이 붙습니다. 검증이 안 되는 작업은 작업이 아닙니다.

### M1 — Hub 코어 (4~6주)

| # | 작업 | 검증 |
|---|---|---|
| 1.1 | 인증 + 프로젝트 CRUD | 가입→프로젝트 생성→로그아웃→재로그인에서 데이터 유지 |
| 1.2 | R2 업로드 + 번들 검증기 (`bundle.json` 스펙) | 잘못된 번들 5종이 각각 다른 에러 메시지로 거부됨 |
| 1.3 | 샌드박스 오리진 배급 (Workers) | S3의 침투 테스트 4개 통과 |
| 1.4 | `/g/{slug}` 플레이 페이지 + 능력 게이트 | `requires` 미충족 기기에서 검은 화면 대신 안내가 뜸 |
| 1.5 | 릴리스 채널(dev/public) + 즉시 롤백 | 롤백이 재빌드 없이 포인터 변경만으로 완료 |
| **M1 완료 기준** | **외부 개발자 1명이 도움 없이 자기 WebGPU 게임을 올려 링크로 플레이시킨다** | |

### M2 — 계측과 리포트 (3~4주)

| # | 작업 | 검증 |
|---|---|---|
| 2.1 | 텔레메트리 SDK + 수집 엔드포인트 | 4개 브라우저에서 이벤트가 스키마대로 도착 |
| 2.2 | 캡처 워커 (스크린샷·폴백 영상·로딩 측정) | 업로드 후 5분 내 산출물 생성. S1의 측정 유효성 구분 표기 |
| 2.3 | 퍼블리시 전 리포트 UI | 의도적으로 망가뜨린 번들에서 4종 경고가 정확히 뜸 |
| 2.4 | 기기 등급표 + 공개 `/compat` | 리포트 500건 이상 집계, 등급 A/B/C 산출 규칙 문서화 |
| **M2 완료 기준** | **크리에이터 1명 이상이 "등급표 보고 버그를 고쳤다"고 말한다** | |

### M2.5 — MCP over Hub (2주) ★ §8 참조

| # | 작업 | 검증 |
|---|---|---|
| 2.5.1 | MCP 서버 골격 + OAuth + 프로젝트 스코프 토큰 | Claude Code에서 연결 성공 |
| 2.5.2 | `project.overview` · `search` · `build.run`(업로드 검증) · 리포트 조회 | CLI에서 "이 빌드 왜 Mali에서 실패해?"에 근거 있는 답이 나옴 |
| **완료 기준** | **랜딩의 Agents 엔트리가 Planned → In build 로 바뀐다** | |

### M3 — 엔진 코어 3D (8~12주)

| # | 작업 | 검증 |
|---|---|---|
| 3.1 | 렌더 그래프 + 클러스터드 포워드 + PBR | 표준 glTF 샘플 3종이 레퍼런스 렌더와 육안 일치 |
| 3.2 | glTF/KTX2 임포트, meshopt, LOD | 50MB glTF가 초기 8MB + 스트리밍으로 로드 |
| 3.3 | ECS + 씬 문서 + 결정적 직렬화 | 저장→로드→저장 바이트 동일 |
| 3.4 | Rapier3D 통합 + 고정 timestep (S5 선행) | 같은 입력 트레이스 100회 최종 상태 동일 |
| 3.5 | 결정적 헤드리스 실행기 | CLI로 N프레임 실행 후 상태 덤프 |
| 3.6 | `@synapse/engine` 패키지 배포 | **손으로 쓴 TS 게임 1개를 M1 경로로 퍼블리시** |
| **M3 완료 기준** | **3.6의 게임이 실제 사용자 기기에서 돌고 M2 등급표에 데이터가 쌓인다** | |

> **여기서 자를 수 있는 것:** 그림자(CSM)와 IBL은 3.1에서 빼고 M5로 미뤄도 게임은 나옵니다. 후처리도 마찬가지입니다. 3D를 택한 이상 PBR과 glTF는 못 빼지만, 조명 품질은 뺄 수 있습니다.

### M4 — 그래프 + 컴파일러 (6~8주)

| # | 작업 | 검증 |
|---|---|---|
| 4.1 | `graph.json` 포맷 + 검증기 | 잘못된 그래프 10종이 노드 ID를 지목하며 거부됨 |
| 4.2 | 노드 라이브러리 v1 (Event/Flow/Pure/Action/Query/Variable/Script) | 각 노드에 단위 테스트 |
| 4.3 | 그래프 → TypeScript 코드젠 | 생성 코드가 타입체크 통과, 같은 그래프 → 같은 바이트 |
| 4.4 | `synapse-graph` CLI (compile / run / test) | **UI 없이 CLI만으로 그래프 게임 1개 빌드·실행** |
| **M4 완료 기준** | **4.4의 게임을 M1 경로로 퍼블리시** | |

### M5 — Studio (10~14주)

| # | 작업 | 검증 |
|---|---|---|
| 5.1 | 에디터 셸 + CRDT 문서 + 자동저장/체크포인트 | 두 탭 동시 편집이 충돌 없이 병합 |
| 5.2 | 3D 뷰포트: 오빗·기즈모·스냅·다중 선택 | 기즈모 조작 결과가 씬 문서에 정확히 반영 |
| 5.3 | 씬 트리 + 인스펙터 | 모든 컴포넌트 타입에 편집 UI |
| 5.4 | 노드 그래프 캔버스 | 노드 500개에서 60fps 유지 |
| 5.5 | 인플레이스 Play + 생성 TS 뷰어 | Play → 정지 → 상태 리셋이 정확 |
| 5.6 | MaterialGraph → WGSL | 머티리얼 3종을 그래프로 작성 |
| **M5 완료 기준** | **코드 한 줄 없이 그래프만으로 3D 미니게임을 완성해 퍼블리시** | |

### M6 — AI 레이어 완성 (5~7주)

| # | 작업 | 검증 |
|---|---|---|
| 6.1 | `graph.patch` · `scene.patch` · `asset.import` · `history` | 각 연산에 대해 잘못된 입력이 문서를 깨뜨리지 못함 |
| 6.2 | `test.headless` (M3.5 위에) | 어서션 실패가 노드 ID로 보고됨 |
| 6.3 | RAG: 심볼 인덱스(결정적) + 문서 임베딩 + 버전 필터 | 구버전 API를 제안하면 테스트가 잡아냄 |
| 6.4 | Studio 내장 에이전트 패널 (같은 툴 표면) | CLI와 패널의 결과가 동일 |
| **M6 완료 기준** | **CLI에서 "적 3마리 추가하고 충돌 시 점수 증가"를 사람 개입 없이 완수하고 헤드리스 테스트를 스스로 통과** | |

---

## 8. 제안 — MCP를 1년 앞당기는 법

원안대로면 MCP는 M6, 즉 **30~45주 뒤**입니다. 제품의 정체성인 "AI 네이티브"가 1년 가까이 화면에 없습니다.

**M2.5를 넣는 이유:** MCP 툴 표면의 어려운 부분은 그래프 편집이 아니라 **전송·인증·스코프·에러 표현**입니다. 그건 Hub만 있어도 만들 수 있습니다. `project.overview`와 `search`, 리포트 조회만 노출해도:

- 툴 표면 설계가 실사용으로 검증됨 (M6에서 재설계할 위험 제거)
- OAuth·토큰·감사 로그가 미리 자리 잡음
- 랜딩의 Agents 엔트리가 Planned를 벗어남
- "AI로 게임 만들기"는 아직 못 해도 **"AI로 내 게임의 호환성 문제를 진단하기"는 됨** — 그 자체로 P0 페르소나에게 유용

비용은 2주이고, M6에서 그만큼 이상 회수됩니다.

---

## 9. 테스트 전략

과하게 만들지 않습니다. 세 종류만 둡니다.

| 종류 | 대상 | 도구 |
|---|---|---|
| 단위 | 번들 검증기, 그래프 검증기·코드젠, 텔레메트리 스키마 | Vitest |
| 골든 | 렌더 출력(레퍼런스 이미지 비교), 코드젠 출력(바이트 비교), 직렬화 왕복 | Vitest + 스냅샷 |
| 결정론 | 같은 입력 트레이스 100회 → 최종 상태 동일 | 헤드리스 실행기 |

**문서 코드 샘플은 CI에서 실제 컴파일합니다.** 깨진 샘플이 RAG 코퍼스에 들어가면 AI가 그대로 재생산합니다 (PLAN.md §5.6).

레이아웃 회귀는 이번 세션에서 쓴 **기하 검사 스크립트**를 `npm run check`로 승격시킵니다 — 가로 오버플로·요소 겹침·눌린 텍스트를 화면 없이 잡습니다. 선택자 충돌과 `min-width:auto` 버그를 실제로 잡아낸 방식입니다.

---

## 10. 비용 — 0원을 유지하는 법

### 10.1 왜 아웃바운드가 문제인가

3D를 택했으므로 게임당 수십~수백 MB입니다. 인기 게임 하나가 요금을 만듭니다.

- 게임 1개 200MB, 월 1,000 플레이 = 200GB
- 일반 클라우드 아웃바운드 ~$0.09/GB → **게임 하나에 월 $18**
- **Cloudflare R2는 아웃바운드를 청구하지 않습니다** → 저장 요금만

이 한 줄이 R2를 고르는 이유 전부이고, 무료 전략에서는 더 중요해집니다. 대역폭 기반 무료 티어(Vercel·Netlify 100GB/월)는 3D 게임 하나로 소진됩니다.

### 10.2 무료를 지키는 설계 결정 2개

**(1) 에셋을 Worker로 통과시키지 않는다.** Workers 무료는 10만 요청/일인데, 게임 하나가 로드될 때 에셋 요청이 수십 건 발생합니다. Worker를 거치면 그게 전부 호출로 계산돼 하루 수천 플레이에서 한도에 닿습니다.

> **번들 에셋은 R2 공개 버킷/커스텀 도메인에서 직접 서빙하고, Worker는 라우팅·인증 판단에만 씁니다.**

**(2) 무료 구간에서의 격리 범위를 정확히 안다.** 도메인을 사지 않으면 게임별 서브도메인은 못 만듭니다. 하지만:

| 격리 | 무료로 가능한가 | 방법 |
|---|---|---|
| 사이트 ↔ 게임 | **가능** | 사이트는 `*.pages.dev`, 게임은 `*.r2.dev` — 서로 다른 오리진 |
| 게임 ↔ 게임 | 불가 | 와일드카드 서브도메인이 필요 → 도메인 구입 (~$10/년) |

M1의 주된 위협은 **업로드된 코드가 우리 세션·API에 접근하는 것**이고, 그건 위 표의 첫 줄로 막힙니다. 게임 간 격리는 게임이 여러 개 생긴 뒤의 문제이므로 도메인 구입 시점까지 미룹니다. S3 스파이크는 이 전제로 검증합니다.

### 10.3 무엇이 가장 먼저 깨지는가

| 순위 | 한도 | 도달 시점 추정 | 대응 |
|---|---|---|---|
| 1 | **R2 저장 10GB** | 200MB 게임 **약 50개** | 무료 계정당 용량 상한(PLAN.md Q4). KTX2 강제. 오래된 빌드 정리 |
| 2 | Workers 10만 요청/일 | 10.2(1)을 지키면 한참 뒤 | 유료 전환 시 $5/월 수준 |
| 3 | GitHub Actions | public 저장소는 무제한 | S1이 GPU를 요구하면 여기서 깨짐 |
| 4 | D1 저장 | 텔레메트리 누적 | 원시 이벤트를 롤업 후 폐기 |

**1번이 결정적입니다.** 3D를 택한 대가가 여기서 나옵니다. 첫 유료 지출은 서버가 아니라 **스토리지**가 될 가능성이 높고, 그 시점이 곧 "게임 50개가 모였다"는 좋은 신호이기도 합니다.

### 10.4 AI 비용

PLAN.md Q2대로 보류하되 제약 둘은 지금부터 지킵니다 — 과금 로직을 에이전트 레이어에 결합하지 않고, **사용량 계측은 M2.5부터 켭니다.** 무료 기조와도 맞습니다: BYO 키 방식이면 우리 지출은 0입니다.

---

## 11. 다음 세 작업

0. **GitHub Pages 켜기** — 저장소 Settings → Pages → Source를 "GitHub Actions"로. 워크플로(`.github/workflows/pages.yml`)는 이미 있음. 이걸 켜야 아래 1번이 배포됨
1. **S2 어댑터 정보 스파이크** — `/status/webgpu` 진단 페이지 작성 후 배포. 가장 싸고(1일) 배포 즉시 데이터가 쌓이기 시작함
2. **S1 헤드리스 캡처 스파이크** — 3일 시간 상자. GitHub Actions 러너에서 시도. 실패하면 무료 전략에서 가장 먼저 깨지는 지점이 여기가 됨(§10.3)
3. **`bundle.json` 스펙 v1 + 검증기** — S4와 함께. M1 전체가 여기 의존

M0가 끝나기 전에는 M1 코드를 쓰지 않습니다. 스파이크 결과가 M1의 설계를 바꿀 수 있습니다.

---

## 12. 이 계획의 알려진 약점

- **M5(Studio)가 가장 크고 가장 불확실합니다.** 3D 에디터는 만들어 보기 전에는 공수를 모릅니다. M4 완료 시점에 M5를 다시 추정해야 합니다.
- **1인 기준 추정이라 병가·번아웃·재작업이 없습니다.** 실제로는 1.5배로 보는 게 맞습니다.
- **S1이 실패하면 M2의 절반이 바뀝니다.** 그래서 3일 안에 답을 냅니다.
- 프레임워크 결정(SvelteKit/Next)을 미뤘습니다. M1 착수 때 결정하되, 더 미루면 그때는 비용이 붙습니다.
