# Synapse — 제품 / 웹사이트 기획서

> 문서 역할 분담
> - `CLAUDE.md` — 작업 원칙 (어떻게 만들 것인가)
> - `DESIGN.md` — 비주얼 시스템 (어떻게 보일 것인가)
> - `PLAN.md` — **제품·웹사이트 기획 (무엇을 만들 것인가)** ← 이 문서
>
> 상태: **v0.3** / 확정: Q1 = Hub 우선(빌드), Q3 = 3D 우선, Q10 = 랜딩 주어는 엔진+AI. 보류: Q2(AI 비용). 나머지 미결은 §11.

---

## 1. 한 줄 정의와 전제

**Synapse = 브라우저에서 노드 그래프로 만들고, AI 에이전트가 같은 그래프를 편집하며, 링크 하나로 배포되는 WebGPU 3D 게임 엔진.**

### 이 문서의 가장 중요한 원칙 — 빌드 순서와 제품 정체성은 별개다

v0.2에서 저지른 착오를 기록해 둡니다. Hub를 먼저 만들기로 하자 제품 정의까지 "배포처"로 바꿔 썼고, 그 결과 랜딩이 **호스팅·진단 서비스**로 읽혔습니다. 블루프린트와 MCP/RAG는 로드맵의 글자로만 남았습니다.

정정합니다.

| | 결정 |
|---|---|
| **빌드 순서** | Hub → Engine → Studio → Agents (변경 없음) |
| **제품 정체성 / 랜딩 주어** | **엔진 + AI.** 처음부터 끝까지 |

Hub를 먼저 만드는 이유는 "그게 제품이라서"가 아니라 **엔진이 겨냥할 하드웨어를 먼저 측정하기 위해서**입니다. 호환성 데이터는 제품이 아니라 엔진의 근거입니다. 랜딩에서 호스팅은 엔진 섹션의 증거로 등장하지, 주어가 되지 않습니다.

대신 정직성 장치가 필수입니다: 모든 섹션에 **Live / In build / Planned** 상태를 표기하고, 미구현 기능을 구현된 것처럼 쓰지 않습니다.

| # | 전제 | 근거 / 확인 필요 |
|---|---|---|
| A1 | 설치 없이 URL만으로 만들고 배포하는 것이 최종 가치 | Unity/Unreal과 정면 승부하지 않는 유일한 축 |
| A2 | 사용자는 두 단계로 온다 — **1단계: 이미 WebGPU 게임을 만든 개발자**(Hub), 2단계: AI로 개발하는 개인 개발자(Studio) | Hub 우선 결정의 직접적 귀결 |
| A3 | AI 에이전트는 **1급 사용자**다 (사람과 동일한 편집 채널을 씀) | §7 전체가 여기서 나옴 |
| A4 | WebGPU 커버리지는 "넓지만 균일하지 않음" — 추정하지 않고 **측정한다** | Hub가 이 측정 장치 자체가 됨 (§5.6) |
| A5 | **Hub는 Studio 없이도 완결적으로 유용해야 한다** | Hub 우선이 성립하는 유일한 조건. 업로드 경로(§9) |
| A6 | 그래픽 축은 3D (glTF · PBR) | 확정 (Q3). 파장은 §6.2 · §9 · §10 · §12 전반 |
| A7 | 초기엔 멀티플레이·서버 게임 로직 없음 | 범위 폭발 방지 (§13) |

---

## 2. 제품 3층 + AI 레이어

```
┌──────────────────────────────────────────────────────┐
│  HUB      플레이 · 발견 · 호환성 · 퍼블리시 · 리믹스   │  synapse.gg/g/{slug}
├──────────────────────────────────────────────────────┤
│  STUDIO   3D 씬 편집 · 블루프린트 · 에셋 · 빌드       │  synapse.gg/s/{project}
├──────────────────────────────────────────────────────┤
│  ENGINE   WebGPU PBR 렌더러 · ECS · 물리 · 오디오     │  런타임 (게임에 임베드)
└──────────────────────────────────────────────────────┘
        ▲ 사람은 Studio UI로,  AI는 MCP로 — 같은 문서를 편집
┌──────────────────────────────────────────────────────┐
│  AI LAYER   MCP 서버 · RAG 인덱스 · 헤드리스 검증     │  mcp.synapse.gg
└──────────────────────────────────────────────────────┘
```

**번들의 두 생산자.** Hub가 호스팅하는 단위는 `Bundle`이고, 생산자는 둘입니다.

```
① 외부 업로드 (P1부터)  : 개발자의 zip  ─┐
                                        ├─→ Bundle ─→ Release ─→ /g/{slug}
② Studio 빌드 (P4부터)  : 그래프→TS→번들 ─┘
```

①을 먼저 열어 두면 Hub는 첫날부터 완결됩니다. ②는 나중에 같은 파이프라인 뒤에 꽂힙니다. **Bundle 스펙을 P1에 확정하는 것이 이 순서 전체의 이음매**입니다.

**설계 핵심 한 문장 (Studio 단계):** 사람의 클릭도, AI의 MCP 호출도 동일한 op 스트림으로 프로젝트 문서에 들어간다. 그래서 AI가 편집하는 동안 에디터가 실시간으로 바뀌고, undo/rollback이 양쪽에 똑같이 작동한다.

---

## 3. 사용자와 핵심 시나리오

### 페르소나

| | **P0. 기존 WebGPU 개발자** (Hub v1 타깃) | P1. AI 개발자 (Studio 타깃) | P2. 플레이어 |
|---|---|---|---|
| 현재 상태 | three.js/Babylon/자체 엔진으로 만들었는데 올릴 데가 마땅찮음 | 아이디어만 있고 도구가 없음 | 클릭 한 번에 놀고 싶음 |
| 통증 | itch.io는 WebGPU를 모름 — 미지원 기기에서 검은 화면, 원인 추적 불가 | 설치·환경설정·러닝커브 | 로딩·미지원·발열 |
| 우리가 주는 것 | 능력 게이트 · 기기별 성능 매트릭스 · GPU 크래시 리포트 | 블루프린트 + MCP 에이전트 | 3초 내 첫 프레임 |
| 성공 신호 | "여기 올리니까 문의 메일이 줄었다" | 한 문장으로 기능 추가 완수 | 재방문 |

> P0은 v0.1엔 없던 페르소나입니다. Hub 우선 결정 때문에 생겼고, **P1보다 먼저 만족시켜야 하는 대상**입니다.

### 핵심 플로우 5개

**F0. 업로드 퍼블리시 (P1 — Hub 단독 가치)**
`/upload` → zip 드롭 → 자동 검증(진입점·용량·CSP) → 헤드리스 스크린샷 자동 캡처 → 미지원 기기용 폴백 영상 자동 녹화 → `/g/{slug}` 링크

**F1. 콜드스타트 → 첫 퍼블리시 (P4 목표: 10분)**
랜딩 라이브 데모 → "Start from template" → Studio → 노드 몇 개 수정 → `Publish`

**F2. 외부 AI 클라이언트로 개발 (CLI 있는 환경)**
MCP 토큰 발급 → `claude mcp add` → "적 스포너 추가" → 그래프 patch → **헤드리스 검증 통과** → Studio 탭 실시간 반영

**F3. CLI 없는 환경 (회사 PC·아이패드·학교)**
Studio AI 패널 → 같은 MCP 툴을 서버사이드 에이전트가 호출 → 편집이 체크포인트로 축적 → `Revert`

**F4. 리믹스 루프 (성장 엔진)**
플레이 → `Remix` → fork → 변형본 퍼블리시 → 원본에 "Remixes 43"
※ P2 시점의 리믹스는 "번들+에셋 fork", P4부터 "그래프 fork"로 승격됩니다.

---

## 4. 사이트맵 / IA

```
/                          랜딩 (라이브 3D WebGPU 데모 히어로)
/explore                   게임 탐색 — 태그·정렬·컬렉션
/g/{slug}                  게임 플레이 페이지
/embed/{slug}              iframe 임베드 전용 (크롬리스)
/u/{handle}                크리에이터 프로필
/upload                    ★ 외부 번들 업로드 (Hub v1의 관문)
/compat                    ★ 공개 WebGPU 호환성 DB (기기·드라이버·성능)
/status/webgpu             내 브라우저 진단 페이지
/templates                 템플릿 갤러리 (P4~)
/jam/{slug}                게임잼 (P6~)

/s/{project}               STUDIO (P4~)
  /s/{project}/scene/{id}      3D 씬 뷰포트
  /s/{project}/graph/{id}      블루프린트 그래프
  /s/{project}/assets          에셋 브라우저
  /s/{project}/build           빌드 & 릴리스 채널

/dashboard                 내 게임/프로젝트 목록
/dashboard/{project}/settings    퍼블리시·도메인·협업자
/dashboard/{project}/compat  ★ 내 게임의 기기별 성능 매트릭스
/dashboard/{project}/ai      MCP 토큰 · 사용량 · 인덱스 상태 (P5~)
/dashboard/{project}/analytics   플레이 수·이탈·FPS 분포

/docs                      문서 (버전별) — RAG 코퍼스 원본
/docs/bundle-spec          ★ 번들 포맷 명세 (P1 필수 — 외부 개발자의 진입 문서)
/docs/api/{version}/{symbol}    자동 생성 API 레퍼런스 (P3~)
/docs/mcp                  MCP 연결 가이드 (P5~)
/pricing  /blog  /changelog  /legal/*
```

★ = Hub 우선 결정으로 v0.2에서 추가/승격된 페이지.

**도메인 분리:** 게임 런타임은 신뢰할 수 없는 코드이므로 `*.play.synapse.gg` 격리 서브도메인에서 실행. Studio도 `studio.synapse.gg`로 분리. (§11-Q7)

---

## 5. 페이지별 스펙

### 5.1 `/` 랜딩

비주얼 시스템은 별도 문서(§ DESIGN.md 개정판)를 따릅니다. 구조만 여기 적습니다.

**히어로가 곧 논지입니다.** 회전하는 도형이 아니라 **블루프린트 그래프 + 프롬프트 + 그에 반응하는 게임 캔버스**가 우측에 들어갑니다. 이 제품에서 아무도 갖지 못한 것은 "브라우저 3D"가 아니라 "사람과 에이전트가 같은 그래프를 편집한다"이고, 히어로는 그걸 보여줘야 합니다.

| 엔트리 | 내용 | 상태 표기 |
|---|---|---|
| 00 | H1 + 그래프/프롬프트/캔버스 데모 + 신뢰 스탯 | — |
| 01 GRF | 그래프가 정본, TS는 생성물. 생성 코드 실물 노출 | In build |
| 02 AGT | MCP 툴 표면 표 + CLI 스니펫 + `test.headless` 검증 루프 | Planned |
| 03 RUN | 런타임 + **호환성 등급표(엔진의 근거로서)** + 퍼블리시 검사 리포트 | Live |
| 04 CAT | 업로드된 외부 게임 카탈로그 | Live |
| 05 SYS | 4단계 로드맵 + 단계별 검증 기준 | — |
| 06 IDN | 아이덴티티 | — |

**정직성 규칙 3개** — 없는 기능을 파는 랜딩은 P0 페르소나를 잃습니다. 그렇다고 있는 것만 팔면 제품 정체성을 잃습니다. 둘 다 피하는 방법:
1. 모든 엔트리에 Live / In build / Planned 상태를 표기한다
2. 데모가 스크립트라면 화면에 그렇게 쓴다 (그래프 편집과 렌더는 실제, 에이전트 응답만 스크립트)
3. 샘플 데이터는 샘플이라고 쓴다

성능 예산: 히어로 3D 씬 **초기 1.2MB 이하(KTX2 필수) / 첫 프레임 2초 이내**. 미지원 시 자동 영상 폴백.

### 5.2 `/g/{slug}` 게임 플레이 페이지

- 상단: 캔버스(기본 16:9, 전체화면), 좌하단 FPS·해상도·어댑터 배지
- 하단: 제목 · 크리에이터 · 태그 · `Play` `Remix` `Share` `Report`
- 우측: 설명, 조작법, **권장 사양(3D라 필수)**, 업데이트 로그
- **미지원 경로:** 검은 화면이나 에러 대신 → 자동 녹화 폴백 영상 재생 + "이 브라우저/기기에서 플레이하려면" 안내 + `/compat` 링크
- 세션 텔레메트리 수집(옵트아웃 가능): 어댑터명·드라이버·`limits`·평균/1% low FPS·디바이스 로스트 여부
- 세이브: `localStorage` (클라우드 세이브는 P6+)
- SEO: 서버 렌더 메타 + OG 이미지(업로드 시 헤드리스 자동 캡처)

### 5.3 `/upload` — Hub v1의 관문

가장 중요한 P1 페이지입니다. 여기서 마찰이 생기면 Hub 우선 전략 전체가 무너집니다.

- zip 드래그앤드롭 → 즉시 클라이언트 검증(진입점 존재·용량·금지 API)
- `manifest.json` 없으면 **자동 추론 후 수정 UI 제시** (작성 강요 금지)
- 업로드 후 자동 처리: 헤드리스 실행 → 스크린샷 3장 · 폴백 영상 15초 · 초기 로딩 시간 측정 · 콘솔 에러 수집
- 결과를 **"퍼블리시 전 리포트"** 로 보여줌: 번들 8.3MB / 첫 프레임 3.1초 / 어댑터 A에서 셰이더 컴파일 실패
- 이 리포트가 곧 제품 가치입니다. itch.io는 zip을 받고 끝냅니다.

### 5.4 `/compat` + `/dashboard/{project}/compat` — 차별점

- 공개 `/compat`: GPU·드라이버·브라우저별 WebGPU 기능/성능 집계 (커뮤니티 자산)
- 크리에이터용: **내 게임의 기기별 매트릭스** — 어떤 GPU에서 몇 fps, 어디서 죽는지
- 이 데이터는 동시에 **우리 3D 엔진의 설계 근거**입니다 (A4). Hub가 P3 엔진의 리스크를 미리 줄여줍니다.

### 5.5 `/s/{project}` Studio 셸 (P4~)

```
┌───────────────────────────────────────────────────────────┐
│ 상단바: 프로젝트명 · Play ▶ · Build · Publish · 협업자      │
├────────┬────────────────────────────────┬─────────────────┤
│ 씬 트리 │  3D 뷰포트 / 그래프 캔버스       │  인스펙터        │
│ 에셋   │  (탭 전환, 분할 아님)            │  AI 패널 (F3)    │
├────────┴────────────────────────────────┴─────────────────┤
│ 하단: 콘솔 · 빌드 로그 · 헤드리스 테스트 결과               │
└───────────────────────────────────────────────────────────┘
```

3D 뷰포트 필수 요소(2D였다면 없었을 것): 오빗/팬/줌 카메라, **트랜스폼 기즈모(이동·회전·스케일)**, 월드/로컬 축 전환, 그리드 스냅, 다중 선택, 뷰 큐브. 에디터 공수의 상당 부분이 여기 들어갑니다.

- `Play ▶`는 별도 창 아닌 뷰포트 인플레이스 실행
- AI 패널은 채팅이 아니라 **작업 로그** — 편집마다 카드, 카드별 `Revert`
- 자동저장 상시. 명시적 `Save` 없음, 대신 이름 붙은 `Checkpoint`

### 5.6 `/docs`

문서는 마케팅 자산이 아니라 **RAG 코퍼스의 원본**입니다. 그래서 제약이 붙습니다.

- 모든 페이지에 머신리더블 frontmatter (`engine_version`, `symbols`, `stability`)
- **모든 코드 샘플은 CI에서 실제 컴파일·실행됨** — 깨진 샘플이 RAG에 들어가면 AI가 그대로 재생산
- API 레퍼런스는 손으로 쓰지 않고 엔진 소스에서 생성
- 버전 셀렉터 필수. 버전 없는 문서는 AI에게 독
- **P1에 필요한 문서는 `/docs/bundle-spec` 단 하나** — 나머지는 엔진이 생긴 뒤

---

## 6. 블루프린트 UI 기획 (P4~)

### 6.1 실행 모델

Unreal Blueprint 관례를 따릅니다 (학습 전이 이득).

- **Exec 핀**(흰색, 실행 순서) + **Data 핀**(타입 색상)
- 노드 분류: `Event`(OnStart/OnTick/OnCollision/OnInput) · `Flow`(Branch/Sequence/ForEach/Delay/Gate) · `Pure`(수학·벡터·쿼터니언, exec 없음) · `Action`(Spawn/Destroy/SetTransform/PlaySound) · `Query`(FindEntity/Raycast) · `Variable`(Get/Set) · `Script`(탈출구, 생 TS)

### 6.2 그래프 종류 (3D 결정 반영)

| 그래프 | Phase | 컴파일 대상 | 3D 결정의 영향 |
|---|---|---|---|
| EventGraph | P4 | TypeScript → ECS 시스템 | 변화 없음 |
| **MaterialGraph** | **P4 (승격)** | WGSL 셰이더 | 2D면 후순위였지만, PBR 머티리얼 변형 없이 3D 게임은 다 똑같아 보임 |
| UIGraph | P6+ | 선언형 UI 트리 | — |
| AnimGraph | P6+ | 상태 머신 | 3D 캐릭터엔 결국 필요. 스키닝/블렌딩 선행 |

### 6.3 정본(source of truth) 결정 — **가장 중요한 결정**

> **그래프 JSON이 정본이다. TypeScript는 생성물이며 읽기 전용이다.**

양방향 라운드트립(코드 수정 → 그래프 역생성)은 넣지 않습니다. 시각 스크립팅 도구가 실패하는 지점이 거의 항상 여기입니다. 임의의 코드를 그래프로 되돌리는 것은 결정적으로 풀 수 없고, 반쯤 되는 순간 사용자는 두 정본 사이에서 데이터를 잃습니다.

탈출구는 명확히 둡니다: `Script` 노드 안의 TS는 사용자 소유이고 컴파일러가 손대지 않습니다. **경계는 노드 단위**로 그어집니다.

생성된 TS는 숨기지 않고 `Generated` 탭에 항상 노출합니다 — AI가 읽고 설명하기 좋고, 블랙박스가 아니라는 신뢰를 줍니다.

### 6.4 AI 협업을 위한 포맷 규칙

AI가 붙는 순간 반드시 필요해지는 4가지:

1. **노드 ID는 안정적 ULID** — 위치·순서가 바뀌어도 불변. diff/merge가 의미를 가짐
2. **의미와 레이아웃 분리** — `graph.json`(의미) / `layout.json`(좌표). AI 편집에 좌표 노이즈가 안 섞임
3. **결정적 직렬화** — 키 정렬 고정, 부동소수 정밀도 고정. 같은 상태 → 같은 바이트
4. **CRDT 문서**(Yjs/Loro) — 사람 편집과 AI 편집의 동시성 자동 해결. A3의 기술적 귀결

---

## 7. AI 레이어: MCP + RAG (P5~)

### 7.1 두 개의 접근 경로, 하나의 툴 표면

```
Claude Code CLI ─┐
Cursor / Desktop ─┼─→ [Remote MCP: mcp.synapse.gg] ─→ 프로젝트 문서 (CRDT)
Studio AI 패널  ─┘         ↑ 동일한 툴 정의
                    [RAG 인덱스] ← 문서 · 프로젝트 심볼 · 템플릿
```

- 전송: **Streamable HTTP MCP** + OAuth 2.1(PKCE), 프로젝트 스코프 토큰
- 방화벽/사내망 대비: `npx @synapse/mcp` 로컬 stdio 브릿지(원격 프록시)
- Studio 내장 에이전트도 **같은 MCP 툴을 서버사이드에서 호출** — 툴 정의가 두 벌이 되면 반드시 갈라집니다

### 7.2 MCP 툴 표면 (v1 제안)

파일 읽기/쓰기 툴을 노출하지 않습니다. **의미 단위 연산**만 노출해야 AI가 문서 구조를 깨뜨릴 수 없습니다.

| 툴 | 설명 |
|---|---|
| `project.overview` | 씬·엔티티·그래프·에셋 요약 트리 (에이전트의 첫 호출) |
| `search` | 하이브리드 검색 — 프로젝트 심볼 + 문서 + 템플릿, 출처 URL 포함 |
| `scene.query` / `scene.patch` | 조회 / `add_entity`·`set_component`·`set_transform`·`reparent`·`delete` |
| `graph.read` / `graph.patch` | 조회 / `add_node`·`connect`·`set_pin_default`·`delete_node` |
| `asset.list` / `asset.import` | 에셋 목록 / glTF·텍스처 임포트 (메타·바운딩박스 자동 추출) |
| `build.run` | 컴파일 → **에러를 노드 ID와 함께 반환** (파일:줄이 아니라) |
| `test.headless` | 입력 트레이스 N프레임 결정적 실행 → 상태 어서션 + 스크린샷 |
| `history.checkpoint` / `history.revert` | 체크포인트 생성 / 되돌리기 |

**`test.headless`가 이 설계의 심장입니다.** CLAUDE.md 원칙 4(목표 주도 실행)를 AI에게 적용하려면 AI가 스스로 "됐는지" 확인할 채널이 있어야 합니다. "적을 추가해줘" → 그래프 patch → 헤드리스 300프레임 → `enemy_count == 3 && score > 0` 검증 → 실패 시 자기 수정. 이 루프가 없으면 AI는 그럴듯한 그래프를 만들고 끝냅니다.

**3D 경고:** 3D 물리(Rapier)의 크로스 플랫폼 결정론은 2D보다 훨씬 까다롭습니다. 고정 timestep + 결정적 빌드 + 물리 스텝 순서 고정이 필요하고, 그래도 GPU 파티클 같은 비결정 요소는 헤드리스 검증에서 배제해야 합니다. P3의 별도 검증 과제입니다.

### 7.3 안전장치

- 모든 mutation 툴은 **자동 체크포인트 생성 후** 실행
- 세션당 변경 예산(노드 N개) 초과 시 사람 승인 요구
- `publish`는 MCP 툴로 노출하지 않음 — 배포는 항상 사람이 버튼을 누름

### 7.4 RAG 설계

**코퍼스 4층**

| 층 | 내용 | 갱신 |
|---|---|---|
| L1 | 엔진 문서 + API 레퍼런스 | 릴리스마다 재색인, **버전 태그 필수** |
| L2 | 현재 프로젝트 (그래프 노드·컴포넌트·에셋·생성 TS) | 저장 시 증분 |
| L3 | 공식 템플릿 · 예제 게임 | 큐레이션 |
| L4 | 커뮤니티 Q&A · 공개 리믹스 패턴 | P6+ |

**검색 전략**
- 하이브리드: BM25 + 임베딩, 심볼 단위 청킹(함수/노드/컴포넌트가 청크 경계)
- L1 결과는 **현재 프로젝트의 엔진 버전으로 하드 필터**. 0.3 API를 0.5 프로젝트에 쓰는 사고가 RAG 게임 도구의 1번 실패 모드
- **API 시그니처는 RAG로 주지 않습니다.** 소스 생성 심볼 인덱스에서 정확 조회. 임베딩은 "무엇을 쓸지" 찾는 데만, 시그니처는 "어떻게 쓸지"이므로 환각 여지를 0으로
- 모든 결과에 인용 URL 포함 → AI 답변이 문서 페이지로 추적 가능

---

## 8. 데이터 모델 (개념 스케치)

```
User ─< Membership >─ Org
Org ─< Project
Project ─┬─ Document(CRDT)  : scenes, entities, graphs, layout, settings   ← P4부터만 존재
         ├─ Asset[]         : blob + meta + 파생본(KTX2/LOD/썸네일)
         ├─ Checkpoint[]    : 스냅샷 (author: user|agent, message)
         ├─ Bundle[]        : 불변 번들 ─ origin: "upload" | "studio-build"
         └─ Release         : channel(dev|beta|public) → Bundle 포인터
Release ─ GamePage(slug, title, tags, og_image, fallback_video, min_spec)
        ─< PlaySession      : adapter, driver, limits, fps_avg, fps_p1, device_lost
Project ─ forkedFrom → Project   (리믹스 계보)
```

두 가지가 v0.1에서 바뀌었습니다.
1. **`Build` → `Bundle`, `origin` 필드 추가.** 업로드와 Studio 빌드가 같은 테이블에 들어가야 Hub 파이프라인을 두 번 만들지 않습니다.
2. **`Document`는 P4부터 nullable.** P1~P3 프로젝트는 문서 없이 번들만 갖습니다.

핵심: **Bundle은 불변, Release는 포인터.** 롤백 = 포인터 변경(즉시, 재빌드 없음).

---

## 9. 퍼블리시 파이프라인

```
① 업로드 (P1)                        ② Studio 빌드 (P4)
   zip → 검증 → 정규화                  그래프 → TS → 번들 → 에셋 최적화
              └──────────┬──────────────────────┘
                    Bundle (콘텐츠 해시, 불변)
                         ↓
            헤드리스 후처리: 스크린샷 · 폴백 영상 · 로딩 측정 · 콘솔 에러
                         ↓
            오브젝트 스토리지 → CDN → dev 채널 자동 배포 (본인만)
                         ↓
            사람이 "Promote to public" → /g/{slug}
```

**3D 결정이 예산을 바꿉니다.** v0.1의 "초기 번들 5MB"는 3D에서 비현실적입니다.
- 초기 씬 **8MB 이하 + 나머지 스트리밍**, 초과 시 퍼블리시 경고(차단 아님)
- **텍스처가 대역폭의 대부분** → KTX2/Basis 필수, 미압축 PNG/JPG는 업로드 시 자동 변환 제안
- 메시는 meshopt/Draco, LOD 생성은 P4 파이프라인
- 무료 티어에 **게임당 용량 상한** 필요(§11-Q4) — 2D였다면 없어도 됐을 제약

게임은 격리 서브도메인 + 엄격한 CSP에서 실행합니다 (사용자 코드 = 신뢰 불가). 업로드 번들은 특히 그렇습니다.

---

## 10. 기술 스택 제안

| 영역 | 선택 | 이유 |
|---|---|---|
| 웹 프레임워크 | Next.js(App Router) 또는 SvelteKit | SEO 필요한 Hub + 앱 라우트 공존 |
| 스토리지 | S3 호환 오브젝트 + CDN | **P1의 핵심 인프라.** 대역폭이 최대 비용 항목 |
| 헤드리스 후처리 | Playwright + GPU 가능 워커 | 스크린샷·폴백 영상·로딩 측정 (§9) |
| DB | Postgres (+pgvector, P5부터) | 텔레메트리·RAG를 별도 인프라 없이 |
| 렌더러 | WebGPU 클러스터드 포워드 + PBR + CSM | 3D 결정 (A6) |
| 3D 에셋 | glTF 2.0 · KTX2/Basis · meshopt | 웹 3D 사실상 표준 |
| 물리 | Rapier3D (WASM) | 결정론 옵션 존재. 검증은 P3 과제 |
| 엔진 언어 | TypeScript + WGSL | 디버깅·AI 가독성 우선 |
| Studio 상태 | CRDT (Yjs 또는 Loro) | §6.4 — 협업이 아니라 **AI 동시편집** 때문에 필수 |
| 그래프 캔버스 | 자체 구현 (Canvas2D/WebGL) | 노드 수천 개 · 커스텀 핀 타입 |
| 빌드 | esbuild (서버) / esbuild-wasm (브라우저 프리뷰) | 동일 시맨틱 |
| MCP | 공식 TypeScript SDK, Streamable HTTP | §7.1 |

**P1에 실제로 필요한 건 위 표의 위쪽 4줄뿐입니다.** 나머지는 P3 이후에 도입합니다.

---

## 11. 결정 현황

### 확정

| # | 질문 | 결정 | 기획 반영 |
|---|---|---|---|
| Q1 | 무엇부터 **만드나** | **웹사이트(Hub) 우선** | §2 두 생산자 · §5.3 업로드 · §12 로드맵 |
| Q10 | 랜딩의 **주어** | **엔진 + AI** — 빌드 순서와 분리 | §1 원칙 · §5.1 전면 개정 |
| Q3 | 2D vs 3D | **3D 우선** | §6.2 MaterialGraph 승격 · §9 예산 상향 · §5.5 기즈모 · §7.2 결정론 경고 |
| Q2 | AI 비용 모델 | **보류** — P5 착수 시 재논의 | 그때까지 제약 2개는 지금 확정 (아래) |

**Q2 보류 중 지켜야 할 제약** — 나중에 어느 쪽을 고르든 재작업이 없도록:
1. 에이전트 레이어에 과금 로직을 결합하지 않는다 (툴 표면은 비용 모델과 무관하게 설계)
2. **첫날부터 사용량을 계측한다** — 과금하지 않아도 세션별 토큰·호출 수를 기록. 나중에 크레딧을 붙일 때 유닛이코노믹스 근거가 이미 있어야 함

### 미결

| # | 질문 | 내 권고 |
|---|---|---|
| Q4 | 무료 티어 한도 | 공개 게임 무제한 + **게임당 용량 상한(예: 500MB)** + 월 대역폭 상한. 3D라 v0.1보다 중요해짐 |
| Q5 | 리믹스 라이선스 기본값 | 퍼블리시 시 명시적 선택 강제. 소급 변경은 지옥 |
| Q6 | WebGPU 폴백 | **WebGL2 폴백 렌더러 만들지 않음.** 대신 능력 게이트 + 영상 폴백 + `/compat` 실측 |
| Q7 | 서브도메인 격리 범위 | Studio/게임 분리는 확정. 게임별 서브도메인은 P1 보안 리뷰 후 |
| Q8 | 제품명·도메인 | `Synapse`는 작업명. 상표 조사 필요 |
| Q9 | **업로드 번들 심사 정책** | Hub 우선 결정으로 새로 생김. 자동 검증만으로 공개할지, 첫 N개는 수동 승인할지. **P1 착수 전 결정 필요** |

---

## 12. 로드맵 (검증 기준 포함)

CLAUDE.md 원칙 4에 따라 각 단계에 **통과/실패가 판정 가능한** 기준을 붙입니다.

**P0 — 기반 검증 (1~2주)**
1. `/status/webgpu` 진단 페이지 (어댑터·limits·기능 리포트) → 검증: 실기기 리포트 30건
2. 히어로용 3D 씬 데모 (glTF 1개 + PBR + 오빗) → 검증: Chrome/Edge/Firefox/Safari 60fps, 초기 1.2MB 이하

**P1 — Hub v1: 업로드 퍼블리시 (4~6주)** ← *Hub 우선 결정의 본체*
1. `/docs/bundle-spec` 확정 + 계정/인증 + 스토리지/CDN + 격리 서브도메인 실행
2. `/upload` 업로드·검증·자동 후처리(스크린샷·폴백 영상·로딩 측정)
3. `/g/{slug}` 플레이 페이지 + 능력 게이트 + 영상 폴백
4. **검증: 나 아닌 외부 개발자가 자기 WebGPU 게임 zip을 올려 링크로 플레이시킨다 (Studio 없이)**

**P2 — Hub v2: 발견 + 호환성 웨지 (4~5주)**
1. `/explore` · `/u/{handle}` · 태그 · `/embed/{slug}`
2. 플레이 세션 텔레메트리 → `/dashboard/{project}/compat` 기기별 성능 매트릭스 + 공개 `/compat`
3. 랜딩 확정(P1~P3 메시지), 번들 fork 수준의 `Remix`
4. **검증: 외부 게임 20개 유치 + 기기 리포트 500건 + 크리에이터 1명 이상이 "매트릭스 보고 버그 고쳤다"**

**P3 — 엔진 코어 3D (6~9주)**
1. 클러스터드 포워드 + PBR + CSM + IBL, glTF/KTX2 임포트, ECS, Rapier3D
2. 결정적 직렬화 + 헤드리스 실행기
3. **검증: 저장→로드→저장 바이트 동일 / 같은 입력 트레이스 100회 최종 상태 동일 (물리 결정론은 별도 판정)**
4. 산출물은 P1 파이프라인에 번들로 투입되어야 함 — 별도 배포 경로 만들지 않기

**P4 — Studio + 블루프린트 (8~12주)**
1. 3D 뷰포트(오빗·기즈모·스냅) + 씬 트리 + 인스펙터
2. EventGraph + MaterialGraph 편집, 인플레이스 `Play ▶`, 서버 빌드
3. **검증: 코드 한 줄 없이 그래프만으로 3D 미니게임 한 판을 완성하고, P1의 퍼블리시 경로로 공개한다**

**P5 — AI 레이어 (5~7주)**
1. MCP 서버 + RAG 인덱스 + `test.headless`
2. **검증: Claude Code CLI에서 "적 3마리 추가하고 충돌 시 점수 증가"를 사람 개입 없이 완수하고 헤드리스 테스트를 스스로 통과**
3. 이 시점에 Q2(비용 모델) 결정

**P6+** — UIGraph, AnimGraph, 게임잼, 클라우드 세이브, 리더보드, 커뮤니티 RAG

### 이 순서의 리스크와 완화

- **차별점이 늦게 온다.** 블루프린트+AI는 P4~P5, 대략 6~9개월 뒤입니다. 그 사이 Hub는 itch.io와 같은 카테고리에 있게 됩니다. → 완화책이 §5.4의 호환성 웨지입니다. "WebGPU를 이해하는 유일한 호스트"는 P1~P3 동안 혼자 서 있을 수 있는 포지션이고, 이건 itch.io가 단기간에 따라오지 않습니다.
- **3D는 P3~P4의 공수를 2D 대비 크게 키웁니다** (에셋 파이프라인 · 기즈모 · 셰이더 그래프 · 물리 결정론). P3 착수 시점에 범위를 다시 자를 각오가 필요합니다.
- **Hub 우선의 숨은 이득:** P1~P2가 수집하는 실기기 데이터가 P3 엔진 설계의 근거가 됩니다. A4의 불확실성이 엔진 착수 전에 해소됩니다. 이 이득은 순서를 뒤집으면 사라집니다.

### 비주얼 시스템 (v0.3에서 전면 교체)

DESIGN.md의 ClickHouse 파생 시스템(검정 캔버스 + 노란 액센트)은 **폐기**했습니다. 게임 플랫폼과 결이 맞지 않았고, 다크 + 액센트 한 색은 AI 생성 디자인의 기본값이라 정체성이 되지 못합니다.

대체 방향은 **The Designers Republic / Wipeout 계보** — 종이색 바탕, 4색 잉크(paper·ink·signal·live), 압축 헤비 디스플레이체, 룬델 배지, 코드가 붙은 엔트리 구조. 근거는 게임 그래픽 헤리티지이지 개발자 툴 사이트가 아닙니다.

핵심 전용 3개:
- Wipeout 기체 스탯 바 → **GPU 계측 미터** (호환성 표가 대시보드가 아니라 스펙 시트가 됨)
- 팀 리버리/코드 → 호스팅된 게임마다 `SYN-01` 코드 + 리버리 색 + 룬델
- 실패는 색이 아니라 **빗금(hatch)** — 잉크를 다섯 번째로 늘리지 않는 규율

아직 비어 있는 것: **Studio용 앱 밀도 토큰.** 마케팅 표면만 정의돼 있고 에디터 UI는 P4 착수 전 별도 정의가 필요합니다.

---

## 13. 범위 밖 (명시적으로 안 함)

멀티플레이/넷코드 · 콘솔·모바일 네이티브 빌드 · 게임 내 결제 · 에셋 스토어 · WebGL2 폴백 · 실시간 음성 협업 · 서버 권위 게임 로직 · **2D 전용 워크플로(스프라이트·타일맵 에디터)** ← 3D 우선 결정으로 P6+ 이후로 밀림

각각 언제 다시 볼지는 P3 착수 시점에 재검토.
